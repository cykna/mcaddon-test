/**
 * This constant represents the distance the entity walks when using applyKnockback with a vector of length = 1 this on xz axis.
 */
export const MAG1_DIST = 1.201;
/**
 * This constant represents the length required for a vector to when using applyKnockback, walk aproximataly, 1 Block of distance, on xz axis. Not applied when Y != 0
 * It's recip(MAG1_DIST)
 */
export const LEN1_BLOCK = 0.832639467111;
